config:
  # This will be checked before running.
  # Set to 0 to run for any amount.
  min_nodes: 1
calls:
  # This will run on every node
  - node: -1
    method: net_PeerCount
  - node: 0
    method: miner_start
